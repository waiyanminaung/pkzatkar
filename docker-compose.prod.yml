services:
  pkmovie-app:
    ports:
      - "80:3000"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
  pkmovie-db:
    restart: always
